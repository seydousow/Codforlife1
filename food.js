// Example one
const tast 

//example 2
class 'lass food{
    constructor(pizza, chickwing, fries, friedfish, noodles)
    this.pizza = pizza;
    this.chickwing = chickwing;
    this.fries = fries;
    this.friedfish = friedfish;
    this.noodles = noodles;

    //example
    powerUp(){
        if(this.powerLevel >= 96){
            this.powerLevel = 100;
            console.log('${this.name} power level is full! special Avillable');
        }else{
            this.powerLevel += 4;
            console.log('${this.name} power level is now at ${this.powerLevel}%');
        }
    }